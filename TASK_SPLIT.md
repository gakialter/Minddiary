# 🤝 AI 协作任务分配清单

## 📊 任务优先级矩阵

| 任务类型 | 紧急程度 | 分配给 | 预计时间 |
|---------|---------|--------|---------|
| 修复 Electron 启动 | 🔴 最高 | Claude | 1-2 小时 |
| 浏览器测试前端 | 🟡 高 | Claude | 30 分钟 |
| UI/UX 优化 | 🟢 中 | Gemini | 1-2 小时 |
| 样式系统重构 | 🟢 中 | Gemini | 1 小时 |
| 后端集成测试 | 🟡 高 | Claude | 1-2 小时 |
| 创意功能设计 | 🟢 中 | Gemini | 1 小时 |

---

## 🎨 给 Gemini 3.1 Pro 的任务清单

### 任务包 A: 样式优化 (优先级: 中)

**前置条件:** 前端可以在浏览器中运行

**任务:**
1. 将 TagManager 组件的内联样式转为 CSS 类
2. 优化整体配色方案（基于 CSS 变量）
3. 添加组件之间的过渡动画
4. 统一按钮、输入框、卡片的圆角和阴影

**输入文件:**
- `src/components/TagManager.jsx` (需要重构)
- `src/index.css` (设计系统)

**输出要求:**
- 更新后的 `TagManager.jsx`
- 扩展的 `index.css`（新增 `.tag-*` 相关类）

### 任务包 B: 视觉设计 (优先级: 低)

**任务:**
1. 设计应用图标（256x256 PNG）
2. 设计 5 种心情的图标/emoji
3. 设计空状态页面（无日记时的引导界面）
4. 设计欢迎页（首次使用）

**输出要求:**
- 设计稿或代码实现
- 可以是 SVG、PNG 或 React 组件

### 任务包 C: 主题系统 (优先级: 中)

**任务:**
1. 设计浅色主题配色
2. 创建主题切换逻辑
3. 添加主题预览功能

**输入文件:**
- `src/index.css`
- `src/components/Settings.jsx`

---

## 🔧 给 Claude 的任务清单

### 任务包 1: 紧急修复 Electron ⭐ (优先级: 最高)

**当前状态:** 阻塞，应用无法启动

**任务:**
1. 使用 `/systematic-debugging` 技能分析问题
2. 创建最小化测试用例
3. 验证 Electron 安装完整性
4. 测试不同版本 (v29, v28)
5. 如果无法解决，提供替代方案

**成功标准:**
- `npm run dev` 可以启动 Electron 窗口
- 能看到 Vite 开发服务器内容

### 任务包 2: 前端验证 (优先级: 高)

**前置条件:** 独立于 Electron

**任务:**
1. 启动 Vite: `npx vite`
2. 在浏览器访问 http://localhost:5173
3. 测试所有 15 个组件是否正常渲染
4. 检查控制台是否有错误
5. 验证路由切换功能

**输出要求:**
- 问题清单（如果有）
- 截图或错误报告

### 任务包 3: 后端测试 (优先级: 高)

**前置条件:** Electron 可以启动

**任务:**
1. 验证 SQLite 数据库创建
2. 测试 IPC 通信（preload.js）
3. 测试所有 API 端点：
   - `entries:getAll`
   - `entries:save`
   - `tags:getAll`
   - `settings:get`
4. 检查文件管理功能

**输出要求:**
- 测试报告
- 发现的 bug 清单

### 任务包 4: 代码审查 (优先级: 中)

**任务:**
1. 审查 `electron/database.js` 的 SQL 注入风险
2. 检查错误处理是否完善
3. 验证 Toast 组件的 `showToast` 导出
4. 检查所有组件的 prop 验证

---

## 🔄 协作工作流

### 方案 1: 并行工作流 (推荐)

```
Claude (主线)          Gemini (支线)
    |                      |
    ├─ 修复 Electron       |
    |                      |
    ├─ 测试前端 ────────→ 接收反馈
    |                      |
    |                   优化 UI/UX
    |                      |
    ├─ 后端测试         样式重构
    |                      |
    └─ 集成 ←──────────── 合并代码
```

### 方案 2: 串行工作流

```
1. Claude: 修复 Electron (阻塞任务)
2. Claude: 验证前端功能
3. Gemini: 优化 UI/UX (基于工作的前端)
4. Claude: 后端集成测试
5. Gemini: 创意功能设计
6. Claude: 最终集成和打包
```

---

## 📋 当前推荐行动

### 立即执行 (Claude)
1. ✅ 已创建任务分配文档
2. ⏭️ 开始修复 Electron 问题
3. ⏭️ 同时在浏览器中验证前端

### 等待 Electron 修复后 (Gemini)
1. 接收前端测试反馈
2. 优化 TagManager 样式
3. 设计应用图标

---

## 💡 提示

**给 Claude 的提示:**
- 使用 `/systematic-debugging` 进行深度调试
- 使用 `/verification-before-completion` 验证修复
- 遇到问题时查阅 `findings.md`

**给 Gemini 的提示:**
- 专注于视觉和用户体验
- 保持与现有设计系统的一致性
- 可以大胆提出创意建议

**协作提示:**
- 使用 Git 分支避免冲突
- 重要决策记录在 `task_plan.md`
- 定期同步进度

---

## 📝 进度追踪

使用此表格追踪任务完成状态：

| 任务 | 负责人 | 状态 | 完成时间 |
|-----|-------|------|---------|
| 修复 Electron | Claude | ⏸️ 待开始 | - |
| 浏览器测试 | Claude | ⏸️ 待开始 | - |
| TagManager 样式 | Gemini | ⏸️ 待开始 | - |
| 应用图标设计 | Gemini | ⏸️ 待开始 | - |
| 后端测试 | Claude | ⏸️ 待开始 | - |
| 主题系统 | Gemini | ⏸️ 待开始 | - |
