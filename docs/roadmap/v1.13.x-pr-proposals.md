# MindDiary v1.13.x PR Proposals

Status: PR-sized plans from v1.13.0 audit evidence.

Schema status: schema unchanged unless a future proposal explicitly opens a schema gate.

## PR Proposal 1: Audit Docs And Task Parity Tests

Suggested title:

`test: cover v1.13.0 chapter task parity and ordering`

Core problem:

- Lock down v1.13.0 chapter task behavior and document system risks after schema 5.

Scope:

- Add audit docs under `docs/audits`.
- Add roadmap docs under `docs/roadmap`.
- Add `tests/studyTaskParity.test.ts`.
- Strengthen `tests/databaseMigrations.test.ts`.

Out of scope:

- No schema change.
- No product UI change.
- No README/release checklist edits unless separately authorized.
- No release/tag/merge.

Acceptance:

- Targeted parity/migration tests pass.
- Full typecheck/test/build validation is attempted and documented.
- Git diff is limited to docs and focused tests.

Recommended release:

- v1.13.1.

## PR Proposal 2: Current-Facing Docs Sync

Suggested title:

`docs: sync v1.13.0 schema and release baseline`

Core problem:

- Current-facing docs still reference older schema/version assumptions.

Scope:

- README data/schema sections.
- Release checklist schema baseline wording.
- Optional superseded notice in historical v2 planning docs.

Out of scope:

- No code/tests unless docs examples require command correction.
- No roadmap rewrite beyond a pointer to the new audit roadmap.

Acceptance:

- Docs mention schema 5, `study_tasks.related_chapter_id`, and v1.13.0 baseline where current-facing.
- Historical docs remain preserved and clearly marked if touched.

Recommended release:

- v1.13.1.

## PR Proposal 3: Low-Risk Ordering Consistency

Suggested title:

`fix: align fallback and sqlite display ordering`

Core problem:

- Non-task display order can differ between Electron SQLite and browser fallback.

Scope:

- Decide and align subject ordering if no schema change is needed.
- Add tie-breakers for Pomodoro aggregate stats.
- Add tie-breakers for mistake list ordering.
- Add fixture tests comparing SQLite and fallback outputs.

Out of scope:

- No custom subject ordering schema unless promoted to v1.14.0.
- No task recommendation changes beyond tests proving non-regression.

Acceptance:

- Equal/tie fixtures are stable.
- Same subject fixture order is identical in Electron and fallback.

Recommended release:

- v1.13.2 if no schema; v1.14.0 if custom persisted subject order is chosen.

## PR Proposal 4: Minor Daily Execution Contract

Suggested title:

`feat: formalize daily execution task ordering contract`

Core problem:

- `getNextTodayAction` depends on ordered input, and source/ordering semantics are spread across API sort and utility selection.

Scope:

- Write a short design note.
- Add tests that prove mixed ordinary/chapter/review/diary task order and recommendation behavior.
- Optionally extract a small shared selector if evidence shows real duplication risk.

Out of scope:

- No AI auto-planning expansion.
- No broad dashboard redesign.
- No schema change unless explicitly approved.

Acceptance:

- Daily execution recommendation order is documented, tested, and stable.

Recommended release:

- v1.14.0.
