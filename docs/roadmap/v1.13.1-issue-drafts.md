# MindDiary v1.13.x Issue Drafts

Status: draft backlog produced from v1.13.0 audit evidence. These are not GitHub issues yet.

Schema status: schema unchanged unless an individual future issue explicitly authorizes a schema change.

## Issue 1: Sync README Current Schema Language To v1.13.0

Type: docs.

Evidence: `README.md` current data section names only the historical v1.11.1/v1.11.2 schema 4 baseline, while `electron/databaseMigrations.ts` defines schema 5 and `RELEASE_NOTES.md` documents `related_chapter_id`.

Scope: current-facing README sections only.

Acceptance: README states current schema 5 and chapter-task attribution without rewriting historical changelog entries.

Recommended version: v1.13.1.

## Issue 2: Update Release Checklist Schema Baseline

Type: docs.

Evidence: `docs/release-checklist.md` says v1.11.3 remains schema 4.

Scope: release checklist baseline wording.

Acceptance: checklist asks to verify `CURRENT_SCHEMA_VERSION` and release plan instead of hardcoding old version.

Recommended version: v1.13.1.

## Issue 3: Mark Older v2 Roadmap As Historical Or Superseded

Type: docs.

Evidence: `docs/v2-roadmap.md` baseline is v1.11.1 and says v1.12.0 may evaluate schema 5.

Scope: add historical notice or superseded pointer.

Acceptance: no current maintainer can mistake it for current baseline.

Recommended version: v1.13.1.

## Issue 4: Mark Older Version Breakdown As Historical Or Superseded

Type: docs.

Evidence: `docs/v2-version-breakdown.md` says v1.12.0 not started and schema 5 may be evaluated.

Scope: add historical notice or superseded pointer only; preserve protected planning content.

Acceptance: current roadmap points to the new v1.13.x-to-v2.0 plan.

Recommended version: v1.13.1.

## Issue 5: Add Manual Chapter Task Smoke Checklist

Type: docs/test process.

Evidence: unit tests cover task/chapter/Pomodoro logic, but packaged/manual smoke was not performed in audit.

Scope: add checklist to audit or release validation docs.

Acceptance: steps cover create subject/chapter, add chapter to today, dashboard recommendation, Pomodoro settlement, chapter deletion.

Recommended version: v1.13.1.

## Issue 6: Keep SQLite/Fallback Task Parity Fixture As Regression Gate

Type: test.

Evidence: `tests/studyTaskParity.test.ts` now proves same-fixture task order/nulling behavior.

Scope: include in normal Vitest suite and avoid weakening projections.

Acceptance: full `npx vitest run` includes the parity tests.

Recommended version: v1.13.1.

## Issue 7: Decide Canonical Subject Ordering

Type: product/technical design.

Evidence: SQLite orders subjects by `name`; fallback orders by `order`.

Scope: decide whether subject order is alphabetical or user-defined.

Acceptance: written decision with tests planned.

Recommended version: v1.13.2 or v1.14.0 if schema required.

## Issue 8: Align Subject Ordering In SQLite And Fallback

Type: bugfix/consistency.

Evidence: confirmed parity difference after Issue 7 decision.

Scope: one chosen ordering behavior in both paths.

Acceptance: same fixture returns same subject ids in Electron repository and fallback API.

Recommended version: v1.13.2 if no schema; v1.14.0 if schema.

## Issue 9: Add Pomodoro Aggregate Tie-Breakers

Type: consistency.

Evidence: Pomodoro stats sort by total minutes without stable secondary key.

Scope: add secondary sort to SQLite/fallback aggregate paths.

Acceptance: equal-total fixture has deterministic order.

Recommended version: v1.13.2.

## Issue 10: Add Mistake List Tie-Breaker

Type: consistency.

Evidence: mistake list orders by `created_at DESC` without id tie-breaker.

Scope: add stable secondary order in SQLite/fallback.

Acceptance: equal-created fixture has deterministic order.

Recommended version: v1.13.2.

## Issue 11: Document `getNextTodayAction` Ordering Contract

Type: docs/test.

Evidence: utility selects first matching task and does not sort internally.

Scope: add test or code comment documenting ordered input requirement.

Acceptance: future callers know to use API-sorted tasks.

Recommended version: v1.13.1.

## Issue 12: Evaluate Shared Task Comparator Extraction

Type: design.

Evidence: SQLite and fallback duplicate status/tie-breaker ordering in different languages/contexts.

Scope: evaluate whether shared constants/helpers are worth it without broad refactor.

Acceptance: written recommendation; implementation only if low risk.

Recommended version: v1.14.0.

## Issue 13: Add LocalStorage Migration Checklist For Future Fields

Type: docs/test process.

Evidence: schema 5 needed browser fallback normalization for `related_chapter_id`.

Scope: add a checklist item for every persisted fallback field.

Acceptance: future schema tasks must add fallback legacy fixture tests.

Recommended version: v1.13.1.

## Issue 14: Add Backup Restore Manual Smoke To Release Checklist

Type: release validation.

Evidence: ZIP restore is unit-tested but high-impact and environment-sensitive.

Scope: manual packaged restore checklist.

Acceptance: checklist validates schema 5 tasks, chapters, attachments, mistake images, and AI key absence.

Recommended version: v1.13.1.

## Issue 15: Verify Browser Fallback AI Key Handling

Type: security audit.

Evidence: Electron safeStorage path is strong; browser fallback settings path was not exhaustively traced in this audit.

Scope: inspect `SettingsContext` browser behavior and add tests if needed.

Acceptance: clear statement whether browser fallback stores keys and whether that is acceptable.

Recommended version: v1.13.2.

## Issue 16: Add Native ABI Troubleshooting Note

Type: docs/devex.

Evidence: local `better-sqlite3` was compiled against Electron ABI and failed under Node until `npm rebuild better-sqlite3`.

Scope: contributor docs or validation log.

Acceptance: docs explain when to run Node rebuild vs Electron rebuild.

Recommended version: v1.13.1.

## Issue 17: Add Release Asset Audit Link To Release Checklist

Type: docs/release.

Evidence: release asset allowlist scripts are strong, but checklist should point to exact script and test.

Scope: docs only.

Acceptance: checklist references `prepare-release-assets.mjs`, `verify-release-metadata.ts`, and release asset tests.

Recommended version: v1.13.1.

## Issue 18: Add Task Timestamp Format Decision

Type: design.

Evidence: SQLite uses `CURRENT_TIMESTAMP`; fallback uses ISO strings.

Scope: decide whether API should normalize timestamp format or keep current behavior.

Acceptance: documented decision; no code change unless user-visible/export issue exists.

Recommended version: v1.13.2.

## Issue 19: Add Import/Backup Duplicate Entry Date Test If Needed

Type: test.

Evidence: entry ordering primarily uses date; normal UI likely one entry per date, imports may differ.

Scope: inspect import/restore behavior for duplicate date possibility.

Acceptance: either document non-issue or add stable-order test.

Recommended version: v1.13.2.

## Issue 20: Add Packaged v1.13.0 Smoke Record Before Any v1.13.1 Release

Type: release validation.

Evidence: audit validated source/tests, not packaged app interaction.

Scope: manual smoke evidence file or release checklist entry.

Acceptance: record commands, app version, schema, create/add/complete/delete chapter task flow, backup restore smoke if included.

Recommended version: v1.13.1 release prep.

## Issue 21: Audit `mockApi` Stub Boundaries

Type: docs/devex.

Evidence: `src/utils/mockApi.ts` is imported before app mount but real fallback behavior lives in `DataContext` APIs.

Scope: document when `mockApi` is used and what it does not guarantee.

Acceptance: contributors do not confuse mock stub behavior with browser fallback parity.

Recommended version: v1.13.2.

## Issue 22: Add Schema-Change Review Template

Type: process.

Evidence: schema 5 touched migrations, backup, fallback, tests, docs.

Scope: add checklist for future schema changes.

Acceptance: template includes migration order, fallback migration, backup/export/restore, old DB fixtures, rollback risk.

Recommended version: v1.13.1.
