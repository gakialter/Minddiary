# 标签样式说明

标签现在由颜色、图标、展示样式和纹理组成。图标支持 emoji 或 1-4 个 Unicode 字符的短符号；展示样式支持 `solid`、`soft`、`outline`、`ghost`；纹理支持 `none`、`dots`、`stripes`、`grid`、`leaf`。

当前“自定义图案”是轻量方案：用户可以填写 emoji / 短符号，并从预设 CSS 纹理中选择。它不包含图片上传、远程图片、自定义 SVG 输入或本地文件路径保存，以避免引入额外的文件安全和路径管理风险。
